import { createSchedulingSessionRepository } from '../../../repositories/schedulingSessionRepository'

/** 全排班畫面共用之工作節 Repository 實例 */
export const schedulingSessionRepository = createSchedulingSessionRepository()
