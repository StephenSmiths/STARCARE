# STARCARE System Engineering Rules (Closed-Loop Development)
## 1. 核心開發哲學：閉環設計 (Closed-Loop Design)
任何功能的開發，必須符合「輸入→邏輯→輸出→審核→反饋」的閉環完整性。
- **閉環分析**：在開發新模組前，AI 必須主動分析並確認此功能的閉環路徑，明確定義：觸發
條件(Input)、業務邏輯(Logic)、狀態變更(Output)、誰來審核(Feedback)、量度標準(Metrics)。
## 2. 架構與結構 (Architecture & Structure)
- **Feature-based Structure**: 嚴格遵守功能模組化 (例如: `/features/scheduling`,
`/features/residents`)。
- **File Size Limit**: 單一檔案嚴禁超過 200 行。若超過，必須立即拆分。
- **Modularization**: 業務邏輯與 UI 必須分離，嚴禁在 UI 中編寫業務計算 (如排班算法)。
- **SOLID Principles**: 執行 SRP，業務邏輯封裝至獨立 `Service` 或 `Hook`。
## 3. 後端與數據策略 (Supabase & Backend)
- **Logic Placement**: 複雜業務邏輯 (如排班算法) 優先放入 `Edge Functions` 或
`PostgreSQL Functions`。
- **Repository Pattern**: 前端嚴禁直接呼叫 Supabase SDK，所有存取通過 `Repository` 層。
- **Observability & Integrity**:
- [cite_start]所有閉環關鍵狀態變更，必須記錄 Audit Trail (操作者ID、時間、變更前狀態、變
更後狀態) 。
- [cite_start]**嚴禁硬刪除 (Hard Delete)**，必須使用 `is_deleted` 或狀態標記實現軟刪除 。
- [cite_start]**防重覆提交**: 所有表單提交與排班確認必須實作 Debounce 或鎖定機制
[cite: 232]。
- **部署與驗收閘門**：Supabase 部署（**`ops:deploy:all`**）見 **`docs/supabase-deploy-runbook.md`**；PAT／部署後自檢與可選 **`npm run ci`** 見 **`docs/security-token-rotation-checklist.md`**（**§D**）、**`docs/go-live-checklist.md`**；文件總覽見 **`docs/business-logic.md`** §0、**`README.md`**；序號清單主檔「**運維與工程**」彙列見 **`docs/pdf-sequenced-gap-checklist.md`**（與 §0 分階交付及 **Stage 2／Phase 3** 歷史追溯路徑對齊；**`docs/phase*.md`**／**`stage*.md`** 開首 **對照** 同鏈該列）；主檔修訂日誌寫入 **`docs/pdf-sequenced-gap-checklist-revision-log.md`**（歸檔段見同目錄 **`pdf-sequenced-gap-checklist-revision-log-2026-05-01a.md`**、**`pdf-sequenced-gap-checklist-revision-log-archive-p2.md`**、**`pdf-sequenced-gap-checklist-revision-log-archive-p3.md`**、**`pdf-sequenced-gap-checklist-revision-log-archive-p4.md`**）；**全案收尾**見 **`README.md`**「專案收尾」及 **`docs/project-completion-*.md`**（與 **`docs/business-logic.md`** §0 **全案收尾執行** 併述 **README** 表前互鏈指引（含各檔 **對照**／**全案收尾母索引**），**`docs/pdf-sequenced-gap-checklist.md`** 開首「全案收尾執行與證據留痕」並讀）；Gate A 自動證據固定入口 **`docs/evidence/gate-a-latest.md`**（檔內 **Next Command** 與 **`preflight:strict`** 並列；**`npm run gatea:evidence:list`**／**`npm run gatea:evidence:refresh`**／**`npm run gatea:evidence:preflight:strict`**）；多數 **`gatea:evidence:*`** 終端 stdout 頁尾 **`scripts/gate-a-markdown-footer.mjs`**（**Export 契約**）。
## 4. 業務一致性與領域聖旨 (Domain Sovereignty)
- **鐵律**: 所有開發決策必須基於 `/docs/business-logic.md` 與 `docs/pdf/` 內客戶簽核 PDF（至少 **`docs/pdf/01-STARCare-核心業務邏輯與-SOP.pdf`**、**`docs/pdf/02-STARCARE-智能院舍照護管理系統.pdf`**、**`docs/pdf/03-STARCARE-工程規範-Closed-Loop.pdf`**；爭議時以 PDF 為準）。
- **規則引用**: 在編寫任何業務邏輯 (如排班 Pass 1-3) 前，必須先在代碼註解中引用對應的
SOP 章節編號。
- **術語規範**: 嚴格遵循上述文件中的業務術語。變數命名優先使用英文對譯 (例如：
`funding_type: 'GradeA_Subsidized' | 'Voucher' | 'Private'`, `SpecialCare_Case`)。
- **語言**: 所有程式碼註釋、文件說明必須使用繁體中文，且維持與業務文件一致性。
## 5. AI 開發執行流程 (The Vibe Workflow)
每次生成代碼前，AI 必須執行以下循環：
1. **結構規劃 (Structure Plan)**：先提出架構，經用戶確認後再生成代碼。
2. **閉環分析 (Closed-Loop Analysis)**：確認功能路徑，並列出量度標準。
3. **自我審查 (Self-Review)**：生成後，必須回報：行數統計、邏輯分離確認、業務一致性確認
(引用SOP)、單元測試狀態。
*若開發過程中出現與業務文件不一致的需求，必須暫停開發並詢問用戶，不得擅自修改業務
邏輯。*