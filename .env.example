VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# 可選：登入後 E2E（`npm run test:e2e:auth`，或連跑 demo 煙霧 `npm run test:e2e:all`；使用 `playwright.auth.config.ts`，建置保留上方 VITE_*）
# Demo 煙霧建置：本機若已設上方 VITE_*，請用 **`npm run build:demo`**（建置時清空 Supabase 變數）再跑 **`PW_PREVIEW_ONLY=1 npm run test:e2e`**，否則 bundle 內嵌真實 URL 會與 CI／TeamLead demo 不一致（見 `playwright.config.ts` 註解）。
# 本機一鍵：**`npm run ci`**（lint → typecheck → vitest → **`build:demo`** → Playwright demo 全套；與 GitHub Actions 同源）
# E2E_AUTH_EMAIL=test@example.com
# E2E_AUTH_PASSWORD=your-test-user-password
# user-role-admin 可選登入 E2E（admin 成功／staff 403）
# E2E_AUTH_ADMIN_EMAIL=admin@example.com
# E2E_AUTH_ADMIN_PASSWORD=your-admin-password
# E2E_AUTH_STAFF_EMAIL=staff@example.com
# E2E_AUTH_STAFF_PASSWORD=your-staff-password
# Gate A（可選）自動產生 403 證據：填入下列後可跑 `gatea:evidence:http`／`gatea:evidence:http:auth`／`gatea:evidence:all`；這些腳本會補上「shell 未設定但本檔已有」的鍵（含 VITE_*）；`gatea:evidence:all` 並將 `{ .env ∪ process.env }` 傳給子程序。
# GATEA_STAFF_EMAIL=staff@example.com
# GATEA_STAFF_PASSWORD=your-staff-password
# 若已取得 staff access_token（例如 DevTools）：`GATEA_STAFF_ACCESS_TOKEN=... npm run gatea:evidence:http`，不必帳密
# GATEA_STAFF_ACCESS_TOKEN=
# 可選：HTTP 取證嚴格檢查（401／403 非預期則 exit 非 0）；等同 `--strict-http`。CI 可設 1 毋須改 npm 參數。
# GATEA_STRICT_HTTP=1
# Gate A 產物路徑以倉庫 docs/evidence/gate-a-latest.md 為準（腳本刷新；該檔檔尾 blockquote 四行定義見下一行註解）；取證步驟見 docs/gate-a-evidence-capture-2026-05-06.md。
# 取證前診斷：`npm run gatea:evidence:preflight`；嚴格（與 README／go-live 並讀）：`npm run gatea:evidence:preflight:strict`。`gatea:evidence:all`／`refresh` 預設先跑 strict preflight；`--no-preflight` 略過。
# 多數 `gatea:evidence:*` 終端 stdout 末兩行與 `scripts/gate-a-markdown-footer.mjs`（檔首 Export 契約）同源；README「Gate A 終端頁尾（維護）」列並讀。
# `gate-a-latest.md` 檔尾 blockquote 四行：`gateALatestMarkdownFooterLines`（見 `docs/gate-a-status-2026-05-06-commands-appendix.md` 下文 latest 段）。
# 服務表單／工作計劃：測試帳之 Auth user_metadata 建議設 starcare_staff_profile_id（對齊 staff 主檔 id），否則僅見「未解析員工編號」提示；`auth-login` E2E 仍以載入成功＋Staff／待審標題為準。

# 1) npm run db:push（含 user_roles 與觸發器）
# 2) Authentication → 建立 Email 使用者；新帳號會自動寫入 user_roles（預設 staff）
# 3) 管理員：Dashboard → Users → raw_app_meta_data 加 "starcare_role":"admin" 後再建帳，或手動 INSERT user_roles
# 4) npm run functions:deploy；Edge 已啟用 verify_jwt，前端須先登入
